import { generateLibByTemplate } from './generate-lib-by-template';

describe('generateLibByTemplate', () => {
  it('should work', () => {
    expect(generateLibByTemplate()).toEqual('generate-lib-by-template');
  });
});
